[phases.install]
cmds = [
  "npm install -g pnpm@9.0.0 && pnpm install --frozen-lockfile=false"
]

[phases.build]
cmds = [
  "npm install -g pnpm@9.0.0 && pnpm --filter backend build"
]

[start]
cmd = "node apps/backend/dist/main.js"